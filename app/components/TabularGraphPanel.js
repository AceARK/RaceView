/*
*	Panel with tabs having 3 types of graph to represent each candidate's data
*	Send data to be translated as graphs using D3 
*/

import React from 'react';

class TabularGraphPanel extends React.Component {
	constructor(props) {
		super(props);
	}

	render() {
		return(
			<h1 className="text-center">TabularGraphPanel</h1>
		);
	}
}

module.exports = TabularGraphPanel;